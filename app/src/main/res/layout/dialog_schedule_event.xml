<?xml version="1.0" encoding="utf-8"?>
<!--
  Event detail overlay (Group 1, 97:120 — 393x283). Renders as a BottomSheet
  with rounded top corners.
-->
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@drawable/bg_card_top"
    android:padding="@dimen/spacing_lg">

    <TextView
        android:id="@+id/event_dialog_title"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:textAppearance="@style/TextAppearance.UniSync.Body"
        android:textSize="@dimen/text_size_title"
        tools:text="Networking Lecture"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:id="@+id/event_dialog_time"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="@dimen/spacing_xs"
        android:textAppearance="@style/TextAppearance.UniSync.Caption"
        tools:text="Wednesday 3 January · 10:00 AM"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/event_dialog_title" />

    <TextView
        android:id="@+id/event_dialog_location"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:textAppearance="@style/TextAppearance.UniSync.Caption"
        tools:text="Hall A · Block 3"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/event_dialog_time" />

    <LinearLayout
        android:id="@+id/event_dialog_actions"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="@dimen/spacing_lg"
        android:orientation="horizontal"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/event_dialog_location">

        <com.google.android.material.button.MaterialButton
            android:id="@+id/event_dialog_edit"
            style="@style/Widget.UniSync.Button.Social.Light"
            android:layout_width="0dp"
            android:layout_height="40dp"
            android:layout_weight="1"
            android:text="@string/action_edit" />

        <Space android:layout_width="@dimen/spacing_sm" android:layout_height="0dp" />

        <com.google.android.material.button.MaterialButton
            android:id="@+id/event_dialog_delete"
            style="@style/Widget.UniSync.Button.Primary"
            android:layout_width="0dp"
            android:layout_height="40dp"
            android:layout_weight="1"
            android:text="@string/action_delete" />
    </LinearLayout>

</androidx.constraintlayout.widget.ConstraintLayout>
